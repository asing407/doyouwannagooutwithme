function getInstagramID() {
    var instagramID = document.getElementById("instagram").value;
    console.log("Instagram ID: " + instagramID);
    return false; // prevent the form from submitting
  }